package pages;

/**
 * @author dhiraj
 *
 */
public class CommonFlows extends BaseActivity {

	public void signUp(String email, String password) {
		// if (helper.isElementPresent("id", anyElement)) {
		// helper.findMobileElement("id", anyElement).click();
		// helper.findMobileElement("id", anyElement).sendKeys(email);
		// helper.findMobileElement("id", anyElement).click();
		// if (helper.isElementPresent("id", anyElement)) {
		// helper.findMobileElement("id", anyElement).click();
		// }
		// helper.findMobileElement("id", anyElement).sendKeys(password);
		// helper.findMobileElement("id", anyElement).click();
		// }
	}
}
